import type { GlossComponentData, GlossConfigData } from '$lib/types.js';

export const defineGlossConfig = (config: GlossConfigData) => config;

export const defineGlossComponent = (config: GlossComponentData) => config;
